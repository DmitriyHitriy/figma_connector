services:
  figma-connector:
    build: .
    container_name: figma-local-file-connector
    environment:
      - REQUIREMENTS_FILE=/app/requirements.txt
    volumes:
      - ./requirements.txt:/app/requirements.txt:ro
      - ./requirements:/app/requirements:ro
    restart: unless-stopped
